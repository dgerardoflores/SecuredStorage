import Storage from './../helpers/Storage.js';
import Crypto from './../helpers/Crypto.js';

class SecuredStorage {
    static initalize(key = null) {
        
    }
};

export default SecuredStorage;